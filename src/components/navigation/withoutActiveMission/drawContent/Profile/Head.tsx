import styled from 'styled-components';

const Head = () => {
  return (
    <StyledHeadWrapper>

    </StyledHeadWrapper>
  );
};

const StyledHeadWrapper = styled.div`
  position: relative;
`;

export default Head;